<Page x:Class="WPF_Mentoring.Pages.Mentorensuche"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Mentorensuche" Background="#272f34">

    <Viewbox Stretch="Uniform" StretchDirection="Both">
        <DockPanel LastChildFill="True" Width="1163">
            <ListView x:Name="employeeListView" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="120"/>
                        <GridViewColumn Header="Stufe" DisplayMemberBinding="{Binding Stufe}" Width="80"/>
                        <GridViewColumn Header="Abteilung" DisplayMemberBinding="{Binding Abteilung}" Width="120"/>
                        <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>



        </DockPanel>
    </Viewbox>
</Page>
